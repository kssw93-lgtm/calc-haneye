import { PolicyArticle } from "@/components/policies/PolicyArticle"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo"; export const metadata = pageMetadata({ title: policyData.earnedIncomeCredit.title, description: policyData.earnedIncomeCredit.summary, path: "/policies/earned-income-tax-credit" }); export default function Page() { return <PolicyArticle data={policyData.earnedIncomeCredit} />; }
