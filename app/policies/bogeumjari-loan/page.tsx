import { PolicyArticle } from "@/components/policies/PolicyArticle"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo"; export const metadata = pageMetadata({ title: policyData.bogeumjari.title, description: policyData.bogeumjari.summary, path: "/policies/bogeumjari-loan" }); export default function Page() { return <PolicyArticle data={policyData.bogeumjari} />; }
