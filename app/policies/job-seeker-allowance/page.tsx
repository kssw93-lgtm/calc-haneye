import { PolicyArticle } from "@/components/policies/PolicyArticle"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo"; export const metadata = pageMetadata({ title: policyData.jobSeekerAllowance.title, description: policyData.jobSeekerAllowance.summary, path: "/policies/job-seeker-allowance" }); export default function Page() { return <PolicyArticle data={policyData.jobSeekerAllowance} />; }
