import { PolicyArticle } from "@/components/policies/PolicyArticle"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo"; export const metadata = pageMetadata({ title: policyData.learningCard.title, description: policyData.learningCard.summary, path: "/policies/national-learning-card" }); export default function Page() { return <PolicyArticle data={policyData.learningCard} />; }
