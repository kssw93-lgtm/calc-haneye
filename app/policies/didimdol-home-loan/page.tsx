import { PolicyArticle } from "@/components/policies/PolicyArticle"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo"; export const metadata = pageMetadata({ title: policyData.didimdol.title, description: policyData.didimdol.summary, path: "/policies/didimdol-home-loan" }); export default function Page() { return <PolicyArticle data={policyData.didimdol} />; }
