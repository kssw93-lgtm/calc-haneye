import { PolicyArticle } from "@/components/policies/PolicyArticle"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo"; export const metadata = pageMetadata({ title: policyData.childTaxCredit.title, description: policyData.childTaxCredit.summary, path: "/policies/child-tax-credit" }); export default function Page() { return <PolicyArticle data={policyData.childTaxCredit} />; }
