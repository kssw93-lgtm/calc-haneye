import Link from "next/link"; import { Container } from "@/components/layout/Container"; import { Breadcrumbs } from "@/components/layout/Breadcrumbs"; import { Card } from "@/components/ui/Card"; import { policyData } from "@/lib/constants/policyData"; import { pageMetadata } from "@/lib/utils/seo";
export const metadata=pageMetadata({title:"청년·신혼부부 주거 지원정책",description:"청년 전세대출, 신혼부부 전세자금, 월세지원과 전세보증료 지원의 최신 확인사항을 안내합니다.",path:"/policies"});
const entries=[["youth-jeonse-loan",policyData.youthJeonse],["newlywed-jeonse-loan",policyData.newlywedJeonse],["jeonse-guarantee-fee-support",policyData.guaranteeFee],["youth-monthly-rent-support",policyData.youthRent]] as const;
export default function Page(){return <Container className="py-10 sm:py-14"><Breadcrumbs items={[{label:"홈",href:"/"},{label:"지원정책"}]}/><h1 className="mt-4 text-2xl font-bold text-ink sm:text-3xl">청년·신혼부부 주거 지원정책</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-ink-soft">계산 결과와 함께 확인할 수 있는 실제 정부·공공기관 지원제도를 정리합니다. 정책별 독립 게시글에 확인일과 공식 신청처를 표시하며 변경·종료 여부를 주기적으로 점검합니다.</p><div className="mt-8 grid gap-5 md:grid-cols-2">{entries.map(([slug,item])=><Link key={slug} href={`/policies/${slug}`}><Card className="h-full"><span className="text-xs font-semibold text-brand">{item.status}</span><h2 className="mt-2 text-lg font-bold text-ink">{item.title}</h2><p className="mt-2 text-sm leading-7 text-ink-soft">{item.summary}</p><p className="mt-3 text-xs text-ink-muted">확인일 {item.checkedAt}</p></Card></Link>)}</div></Container>}
