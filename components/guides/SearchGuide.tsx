import Link from "next/link";
import { CalculatorCTA } from "@/components/guides/CalculatorCTA";
import { Breadcrumbs } from "@/components/layout/Breadcrumbs";
import { Container } from "@/components/layout/Container";

export interface SearchGuideData { title: string; intro: string; category: string; categoryHref: string; facts: string[]; sections: { title: string; body: string }[]; sources: { name: string; url: string }[]; links: { label: string; href: string }[] }
export function SearchGuide({ data }: { data: SearchGuideData }) { return <Container className="max-w-3xl py-10 sm:py-14"><Breadcrumbs items={[{ label: "홈", href: "/" }, { label: data.category, href: data.categoryHref }, { label: data.title }]} /><article><h1 className="mt-4 text-2xl font-bold text-ink sm:text-3xl">{data.title}</h1><p className="mt-4 text-base leading-8 text-ink-soft">{data.intro}</p><p className="mt-3 text-xs text-ink-muted">최종 내용 검토: 2026년 9월 2일</p><section className="mt-8 rounded-card border border-hairline bg-surface-subtle p-6"><h2 className="text-lg font-bold text-ink">계산 전에 확인할 조건</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm leading-7 text-ink-soft">{data.facts.map((fact) => <li key={fact}>{fact}</li>)}</ul></section><div className="mt-10 space-y-10">{data.sections.map((section, index) => <div key={section.title}><section><h2 className="text-xl font-bold text-ink">{section.title}</h2><p className="mt-3 whitespace-pre-line text-sm leading-7 text-ink-soft">{section.body}</p></section>{index === Math.min(1, data.sections.length - 1) && <CalculatorCTA hrefs={data.links.map(link => link.href)} />}</div>)}</div><section className="mt-10 rounded-card border border-brand/20 bg-brand-light p-6"><h2 className="text-lg font-bold text-ink">직접 계산하고 더 알아보기</h2><div className="mt-3 flex flex-wrap gap-x-5 gap-y-3">{data.links.map((link) => <Link key={link.href} href={link.href} className="font-semibold text-brand hover:underline">{link.label} →</Link>)}</div></section><section className="mt-10 border-t border-hairline pt-6"><h2 className="text-lg font-bold text-ink">공식 출처</h2><ul className="mt-3 space-y-2 text-sm">{data.sources.map((source) => <li key={source.url}><a href={source.url} target="_blank" rel="noreferrer" className="font-medium text-brand hover:underline">{source.name} ↗</a></li>)}</ul><p className="mt-3 text-xs leading-6 text-ink-muted">예시는 입력 조건을 단순화한 참고값입니다. 실제 상품·계약·근로관계에는 공식 기관의 최신 기준을 확인하세요.</p></section></article></Container>; }
