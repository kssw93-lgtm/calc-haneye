import Link from "next/link";
import { BookOpen } from "lucide-react";
import { Breadcrumbs } from "@/components/layout/Breadcrumbs";
import { Container } from "@/components/layout/Container";
import { Card } from "@/components/ui/Card";
export function GuideHub({ title, description, flow, items }: { title: string; description: string; flow: string; items: { title: string; description: string; guideHref: string; calculatorHref: string }[] }) { return <Container className="py-10 sm:py-14"><Breadcrumbs items={[{ label: "홈", href: "/" }, { label: "계산 가이드", href: "/guides" }, { label: title }]} /><h1 className="mt-4 text-2xl font-bold text-ink sm:text-3xl">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-relaxed text-ink-soft sm:text-base">{description}</p><div className="mt-6 rounded-card border border-brand/20 bg-brand-light p-5 text-sm leading-relaxed text-ink-soft">{flow}</div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{items.map((item) => <Card key={item.guideHref} className="flex h-full flex-col"><BookOpen className="h-6 w-6 text-brand" aria-hidden="true" /><h2 className="mt-3 text-base font-bold text-ink">{item.title}</h2><p className="mt-2 flex-1 text-sm leading-relaxed text-ink-soft">{item.description}</p><div className="mt-4 flex flex-wrap gap-3"><Link href={item.guideHref} className="text-sm font-semibold text-brand hover:underline">가이드 →</Link><Link href={item.calculatorHref} className="text-sm font-semibold text-brand hover:underline">계산기 →</Link></div></Card>)}</div></Container>; }
