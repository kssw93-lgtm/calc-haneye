import Link from "next/link";
import { Breadcrumbs } from "@/components/layout/Breadcrumbs";
import { Container } from "@/components/layout/Container";
import { absoluteUrl } from "@/lib/utils/seo";

export interface GuideData { path: string; title: string; intro: string; sections: { title: string; paragraphs: string[] }[]; calculatorHref: string; calculatorLabel: string; related: { href: string; label: string }[] }
export function GuideArticle({ data }: { data: GuideData }) { const jsonLd = { "@context": "https://schema.org", "@type": "Article", headline: data.title, description: data.intro, mainEntityOfPage: absoluteUrl(data.path) }; return <Container className="py-10 sm:py-14"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><Breadcrumbs items={[{ label: "홈", href: "/" }, { label: "계산 가이드", href: "/guides" }, { label: data.title }]} /><article className="mx-auto mt-4 max-w-3xl"><h1 className="text-2xl font-bold text-ink sm:text-3xl">{data.title}</h1><p className="mt-4 text-base leading-8 text-ink-soft">{data.intro}</p><div className="mt-10 space-y-10">{data.sections.map((section) => <section key={section.title}><h2 className="text-xl font-bold text-ink">{section.title}</h2>{section.paragraphs.map((paragraph) => <p key={paragraph} className="mt-3 text-sm leading-7 text-ink-soft">{paragraph}</p>)}</section>)}</div><div className="mt-12 rounded-card border border-brand/20 bg-brand-light p-6"><h2 className="text-lg font-bold text-ink">직접 확인해 보세요</h2><Link href={data.calculatorHref} className="mt-3 inline-block text-sm font-semibold text-brand hover:underline">{data.calculatorLabel} →</Link></div><div className="mt-8 flex flex-wrap gap-4">{data.related.map((item) => <Link key={item.href} href={item.href} className="text-sm font-semibold text-brand hover:underline">{item.label} →</Link>)}</div></article></Container>; }
