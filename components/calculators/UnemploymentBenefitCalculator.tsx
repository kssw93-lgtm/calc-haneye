"use client";
import { useState, type FormEvent } from "react";
import { calculateUnemploymentBenefit, type InsuredPeriod } from "@/lib/calculators/unemploymentBenefit";
import { formatWon } from "@/lib/utils/currency";
export function UnemploymentBenefitCalculator(){const [result,setResult]=useState<ReturnType<typeof calculateUnemploymentBenefit>|null>(null);const [error,setError]=useState("");function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");const data=new FormData(event.currentTarget);try{setResult(calculateUnemploymentBenefit({averageMonthlyWage:Number(data.get("wage")),dailyHours:Number(data.get("hours")),insuredPeriod:String(data.get("period")) as InsuredPeriod,olderOrDisabled:data.get("older")==="yes"}))}catch(caught){setResult(null);setError(caught instanceof Error?caught.message:"입력값을 확인해 주세요.")}}return <div className="mt-8 grid gap-6 lg:grid-cols-2"><form onSubmit={submit} className="space-y-5 rounded-card border border-hairline bg-white p-6"><Field label="퇴직 전 평균 월급" name="wage" value="3000000" suffix="원"/><Field label="1일 소정근로시간" name="hours" value="8" suffix="시간"/><label className="block text-sm font-bold">고용보험 가입기간<select name="period" className="mt-2 min-h-12 w-full rounded-lg border border-hairline px-3"><option value="under1">1년 미만</option><option value="1to3">1년 이상 3년 미만</option><option value="3to5">3년 이상 5년 미만</option><option value="5to10">5년 이상 10년 미만</option><option value="over10">10년 이상</option></select></label><label className="block text-sm font-bold">퇴직 당시 만 50세 이상 또는 장애인<select name="older" className="mt-2 min-h-12 w-full rounded-lg border border-hairline px-3"><option value="no">아니오</option><option value="yes">예</option></select></label>{error&&<p className="text-sm text-danger">{error}</p>}<button className="min-h-12 w-full rounded-lg bg-brand font-bold text-white">예상액 계산하기</button></form><section aria-live="polite" className="rounded-card border border-hairline bg-surface-subtle p-6"><h2 className="text-lg font-bold">모의계산 결과</h2>{result?<dl className="mt-5 space-y-4">{[["1일 예상 구직급여",formatWon(result.dailyBenefit)],["예상 지급일수",`${result.benefitDays}일`],["총 예상액",formatWon(result.totalBenefit)],["적용 하한액",formatWon(result.floor)],["2026년 상한액",formatWon(result.ceiling)]].map(([label,value])=><div className="flex justify-between gap-4" key={label}><dt className="text-sm text-ink-soft">{label}</dt><dd className="text-right font-bold">{value}</dd></div>)}</dl>:<p className="mt-4 text-sm text-ink-muted">월급과 가입기간을 입력하면 예상액을 표시합니다.</p>}<p className="mt-6 text-xs leading-6 text-ink-muted">월급을 30일로 나눈 단순 평균임금 추정입니다. 실제 평균임금, 피보험단위기간, 퇴사 사유와 수급자격은 고용센터 심사 결과가 우선합니다.</p></section></div>}
function Field({label,name,value,suffix}:{label:string;name:string;value:string;suffix:string}){return <label className="block text-sm font-bold">{label}<div className="relative mt-2"><input name={name} type="number" min="1" defaultValue={value} className="min-h-12 w-full rounded-lg border border-hairline px-3 pr-16"/><span className="absolute right-3 top-1/2 -translate-y-1/2 text-sm text-ink-muted">{suffix}</span></div></label>}
